export interface RegisterDTO {
    firstName: string;
    lastName: string;
    phoneNumber: string;
    email: string;
    password: string;
    pin: string;
}

export interface RegisterResponseDTO {
    firstName: string;
    lastName: string;
    phoneNumber: string;
    email: string;
}